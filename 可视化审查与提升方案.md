# 可视化审查与提升方案（面向高水平学术论文发表）

> 适用对象：文稿《可再生能源发展驱动与阻碍因素的量化研究：基于585份文档的文本挖掘分析》
>
> 当前文稿的主要问题不是“图画得不够花”，而是**统计口径、可复现性与发表级图形规范**尚未闭环：
> - 图形大多只呈现均值/计数，缺少分布、置信区间与稳健性；
> - “文档量”“条目量（抽取实例数）”在实现中存在混用风险；
> - 年份范围在代码中被硬编码为 2015–2025，与 2005–2024 的叙述存在不一致风险；
> - 输出分辨率与格式未达到常见期刊的“可直接排版”标准（如 300–600 dpi、PDF/SVG 矢量）。

---

## 1. 文档图片链接审查（逐图定位到生成脚本）

文稿中共引用 6 张图片，均来自同一输出目录：`data/output/analysis/`。

| 图号（文稿） | 图片链接（md） | 文件是否存在 | 生成脚本 | 生成函数 | 当前图形类型 |
|---|---|---:|---|---|---|
| 图1 | `data/output/analysis/driver_strength.png` | ✅ | `analyze_structured_results.py` | `plot_driver_strength()` | 横向条形图（均值） |
| 图2 | `data/output/analysis/barrier_severity.png` | ✅ | `analyze_structured_results.py` | `plot_barrier_severity()` | 横向条形图（均值） |
| 图3 | `data/output/analysis/driver_barrier_timeline.png` | ✅ | `analyze_structured_results.py` | `plot_driver_barrier_timeline()` | 堆叠面积图 |
| 图4 | `data/output/analysis/pestel_heatmap.png` | ✅ | `analyze_structured_results.py` | `plot_driver_barrier_heatmap()` | 2列热力图（均值） |
| 图5 | `data/output/analysis/sector_driver_heatmap.png` | ✅ | `analyze_structured_results.py` | `plot_sector_driver_heatmap()` | 行业×类别共现热力图 |
| 图6 | `data/output/analysis/metrics_coverage.png` | ✅ | `analyze_structured_results.py` | `plot_metric_counts()` | Lollipop（Top30） |

补充：仓库中已经存在更“高级可视化”的实现雏形（但当前文稿未使用）：
- `generate_deep_analysis_visualizations.py`：包含桑基图、网络图、主题河流图等（依赖可选 plotly/networkx）。
- `src/analysis/academic_visualizer.py`：定义了更偏“发表级”的 Matplotlib 风格与多种图形模板（dpi=300、colorblind palette 等）。

---

## 2. 现有可视化的关键不足（能否支撑高水平论文的根因）

### 2.1 统计口径与论文叙述存在“不一致风险”（必须优先解决）

1) **“文档量” vs “抽取条目量”混用**
- 例如：`plot_driver_barrier_timeline()` 里用 `driver_df` / `barrier_df` 的行数按年统计，而 `driver_df` 的一行是“驱动条目实例”（同一文献可贡献多行）。
- 但文稿图3标题/文字表述为“文档量时间演化”。
- 这会导致：长文/信息密集文献被隐式加权，趋势可能被夸大。

2) **年份范围被硬编码为 2015–2025**
- `plot_driver_barrier_timeline()` 与 `plot_metric_trends()` 中均有年份过滤：`(year >= 2015) & (year <= 2025)`。
- 文稿方法部分将研究时间设为 2005–2024，并且在引言与结果中讨论 2005–2015 前后的变化；硬编码会直接削弱叙事一致性与可复核性。

3) **仅展示“均值”，缺少样本量与分布信息**
- 图1/图2/图4均为按类别均值聚合；没有展示每个维度的样本量 n、方差/分布形态。
- 在审稿语境下，这会被质疑为：结论对极端值敏感、对抽取误差敏感、对文本长度敏感。

### 2.2 发表级图形规范不足（会在排版环节暴露）

- 输出仅 PNG、dpi=200；多数期刊建议图稿 300–600 dpi，或优先提供 PDF/EPS/SVG（矢量）。
- 图形尺寸未对齐期刊版式（单栏/双栏宽度、字号、线宽、标注密度）。
- 热力图 `annot=True` 在类别较多时容易“数值糊成一片”，视觉信息密度不符合顶刊风格。
- 配色跨图不一致，缺少统一的“语义配色体系”（例如驱动=绿色、阻碍=红色；PESTEL 六维固定颜色）。

### 2.3 学术叙事不够“高级”的可视化原因

现在的图更多是“描述性统计图”，而高水平论文通常需要：
- **机制线索**：因素之间的结构关系（共现网络、因果链路、政策→市场→技术的链条）；
- **不确定性**：抽取误差、样本差异、时间窗口、不同聚合策略下结论是否稳健；
- **对照与归一化**：行业规模差异、文献数量差异、地区覆盖差异导致的偏差是否被控制。

---

## 3. 逐图改造建议（在不改变研究主题前提下，把图升级到“可投顶刊”）

下面给出“最小改造路径”：不要求引入全新模型，只把统计口径、图形语言与不确定性补齐。

### 图1（驱动强度分布）——从“均值条形图”升级为“分布 + 置信区间”

现状问题：仅均值，缺少分布与 n。

建议替代图形（任选其一，按审稿偏好）：
- **Violin/Box + 点（strip/swarm）+ 均值/中位数 + 95% bootstrap CI**；
- 或 **森林图（Forest plot）**：每个维度一个点估计 + CI，并标注 n。

统计口径建议：
- 同时报告两种口径：
  - 条目级（instance-level）：每个抽取条目为样本；
  - 文献级（record-level）：每篇文献对某维度取均值/最大值后再统计，避免长文隐式加权。

### 图2（阻碍严重度分布）——与图1保持同构

现状问题：与图1相同。

建议：
- 图2使用与图1一致的图型与配色规则（驱动/阻碍语义一致、便于对比）。

### 图3（驱动 vs 阻碍 时间演化）——先修正“文档量”口径，再谈高级风格

现状问题：
- 现在画的是“条目量时间线”（同一文献多个条目会被多计）；
- 过滤年份 2015–2025。

建议：
- 改为按 `record_id`（或文本记录唯一标识）去重后统计“每年涉及驱动/阻碍的文献数”；
- 同时提供：
  - 绝对数量（文献数）
  - 相对比例（驱动占比 vs 阻碍占比），避免样本量变化驱动趋势。
- 增强版：增加 **变点检测（change point）标记** 或分段趋势线（对应《巴黎协定》/政策节点）。

### 图4（PESTEL 驱动 vs 阻碍 对比）——从“二维热力图”升级为“结构化对照面板”

现状问题：
- 只有均值；
- 维度顺序按字母排序，不符合 PESTEL 叙事顺序；
- 数值标注密集，审稿人读图成本高。

建议：
- 固定 PESTEL 顺序：政治→经济→社会→技术→环境→法律；
- 采用双面板：左（驱动森林图）、右（阻碍森林图），共享同一维度轴；
- 或者保留热力图但：
  - 去掉 `annot` 或只标注 Top-k；
  - 用聚类排序揭示结构（如果样本量足够）。

### 图5（行业×驱动类别共现）——从“计数热力图”升级为“归一化 + 结构关系图”

现状问题：
- 计数未归一化：高频行业天然更亮，可能只是因为样本多。

建议：
- 先给出 **归一化版本**：
  - 行内归一化（每行业内占比），回答“该行业更偏向哪些驱动类别”；
  - 或列归一化（该驱动类别主要出现在什么行业）。
- 高级版（更像顶刊）：
  - **二部图（bipartite graph）/Chord diagram** 展示行业—类别的连接强度（静态可用 networkx + circular layout）。

### 图6（量化指标覆盖）——从 Top30 展示升级为“长尾结构 + 归一化词表”

现状问题：
- 只画 Top30，无法证明“长尾”结构；
- 指标命名缺少归一化（同义、口径、单位），导致可重复性弱。

建议：
- 增加一张补充图（或同图两面板）：
  - Rank-frequency（对数坐标）展示 Zipf/长尾；
  - Top-k 仍用 lollipop，但标注同义合并后的“标准指标名”。
- 引入 **指标词表归一化**：同义词合并 + 单位规范化 + 口径标注。

---

## 4. 工程与方法升级路线（分三档：快速可发表 / 高级可投顶刊 / 可复现研究资产化）

### A. 1–2 天：快速把“可发表最低线”补齐（强烈建议先做）

目标：不改变研究设计，只修复口径与输出规范，让图能进入论文排版。

- 修正时间范围：统一为 2005–2024（或由参数控制），移除 2015–2025 硬编码。
- 修正文档量统计：时间线按文献/记录去重（document-level）。
- 输出规范：
  - 默认 `dpi=300`（更建议 450/600，按期刊要求）；
  - 同时输出 `png + pdf`（或 `svg`），并统一 `bbox_inches='tight'`。
- 统一风格：
  - 白底（facecolor=white），减少期刊排版冲突；
  - 使用 colorblind palette；
  - 固定 PESTEL 配色与顺序。
- 图内标注：每个维度标注 n（样本量），减少“均值幻觉”。

### B. 3–7 天：把“描述统计”升级为“可解释 + 可审稿”的学术图组

目标：让图能够支撑机制讨论、稳健性与理论贡献。

- 不确定性：
  - bootstrap CI（条目级与文献级各一套）；
  - 或者分布图（violin/box）替代纯均值条形。
- 稳健性：
  - 同时报告 mean / median；
  - 剔除低置信度抽取条目（high/medium/low）后的对比；
  - 按文献来源（期刊/会议/报告）、语言（中/英）分层对比。
- 结构关系图：
  - 共现网络（因素/类别之间），给出中心性/社区；
  - 因果链从“矩阵”升级为 “Sankey/Alluvial”。

### C. 2–4 周：形成可复现、可持续扩展的“论文级可视化资产”

目标：让可视化成为研究管线的一部分：数据→统计→图→论文，一键重跑。

- 建立 Figure Manifest（图形清单）：每张图的
  - 数据来源文件（structured json/jsonl）、过滤条件、聚合口径、随机种子、输出路径、生成时间；
  - 自动写入 `figures_manifest.json`，便于审稿复核。
- 统一 Figure Factory：把所有图的尺寸、字体、颜色、输出格式集中到一个配置（YAML/py config）。
- 论文版式适配：提供单栏/双栏尺寸预设（例如 85mm / 180mm 宽度），字号统一。

---

## 5. 建议的“下一版图组清单”（可直接对齐论文结构）

1) PESTEL 维度：驱动/阻碍的分布与 CI（主图，替代图1+图2的均值呈现）
2) 时间演化：文献级趋势 + 驱动/阻碍占比 + 变点标记（主图，替代图3）
3) 行业异质性：归一化行业×类别矩阵 + 聚类（主图，升级图5）
4) 指标体系：长尾结构 + 指标归一化前后对比（主图或补充图，升级图6）
5) 机制图（补充）：Sankey（政策→市场→技术→部署）/ 因果链网络
6) 质量控制（补充）：抽取置信度分布、不同阈值下结论一致性

---

## 6. 代码层面的落地点（如何在当前仓库中最省力实现）

- 现有文稿引用的图全部出自：`analyze_structured_results.py`。
  - 建议在该脚本内先完成 A 档修复（年份范围、文档量口径、输出格式/分辨率/风格）。
- 如果希望直接使用更高级模板，可复用/迁移：
  - `src/analysis/academic_visualizer.py` 的“发表级风格与图形模板”；
  - `generate_deep_analysis_visualizations.py` 的桑基图/网络图实现（需要确认输入数据字段一致）。

---

## 7. 验收标准（建议用于自检/投稿前检查）

- 叙述与统计口径一致：图3若写“文档量”，代码必须按文献去重统计。
- 图稿规范：
  - 300–600 dpi PNG（或 TIFF）
  - 至少一份矢量（PDF/SVG/EPS）
  - 字体嵌入/可替换（中文不乱码）
- 每张图都有：清晰标题、轴标签、单位/尺度说明、样本量 n（必要时）与不确定性表达。
- 可复现：同一输入数据、同一版本代码，重跑图形一致；输出目录包含 manifest。

